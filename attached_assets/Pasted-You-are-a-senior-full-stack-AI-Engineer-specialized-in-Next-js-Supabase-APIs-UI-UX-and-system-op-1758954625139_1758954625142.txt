You are a senior full-stack AI Engineer specialized in Next.js, Supabase, APIs, UI/UX, and system optimization.  
Your task: **Scan, Debug, and Auto-Fix the entire Zyra application** so it runs smoothly with no errors, fast loading, and full functionality.  

### Step 1: Error & Dependency Audit
- Identify all frontend and backend errors (UI bugs, Supabase issues, missing env vars, API failures, console warnings, runtime crashes).  
- Check for unused imports, broken routes, unhandled promises, null/undefined references.  
- Verify Supabase schema alignment with frontend queries and mutations.  
- Ensure all `.env` variables (VITE_SUPABASE_KEY, SUPABASE_URL, API keys) are correctly used.  

### Step 2: Fix & Refactor
- Provide **fixed code snippets** with clear file/folder names and updated functions.  
- Resolve TypeScript errors (if used).  
- Remove duplicate logic, ensure proper error handling with `try/catch`.  
- Fix async/await misuse and API call failures.  
- Ensure all buttons, tabs, modals, and components in Zyra UI work correctly.  
- Make the UI responsive with consistent **Cyberwave theme** (Base: #0D0D1F, Primary: #00F0FF, Secondary: #00FFE5, Accent: #FF00F5).  

### Step 3: Performance Optimization
- Add loading states and skeletons for Supabase/API fetches.  
- Optimize bundle size (tree-shaking, code splitting).  
- Fix hydration errors in Next.js (if any).  
- Use proper caching & revalidation strategies.  

### Step 4: Final Validation
- Run end-to-end checks: login/signup, Supabase CRUD ops, API calls, UI navigation, feature toggles.  
- Verify no console errors/warnings remain.  
- Confirm smooth transitions, animations, and fast page loads.  

### Output Format:
1. **Error Report** – list all errors/warnings found.  
2. **Fixed Code** – corrected snippets with file paths.  
3. **Optimization Suggestions** – optional improvements for speed, scalability, security.  
4. **Validation Checklist** – confirm all systems work (✅/❌).  

Your final output should ensure Zyra is **100% production-ready**: no errors, no broken UI, optimized performance, smooth user experience.
